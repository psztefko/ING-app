<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="DarkTheme" parent="AppTheme">
        <item name="primaryTextColor">@android:color/primary_text_dark</item>
        <item name="secondaryTextColor">@android:color/secondary_text_dark</item>
        <item name="bgColor">#121212</item>
    </style>

    <style name="LightTheme" parent="AppTheme">
        <item name="primaryTextColor">#121212</item>
        <item name="secondaryTextColor">@android:color/secondary_text_dark</item>
        <item name="bgColor">@android:color/background_light</item>
    </style>

</resources>
